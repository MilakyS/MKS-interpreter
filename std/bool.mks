var true =: 1;
var false =: 0;
fnc not(x) -> if (x ?= 0) -> return 1; <- return 0; <-
fnc and(a,b) -> if (a ?= 0) -> return 0; <- if (b ?= 0) -> return 0; <- return 1; <-
fnc or(a,b) -> if (a ?= 0 && b ?= 0) -> return 0; <- return 1; <-
